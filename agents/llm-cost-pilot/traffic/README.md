Run `python prepare.py generate` to create synthetic test traffic.

This generates `sample.jsonl` with 1000 synthetic LLM API requests spanning five complexity tiers (nano, small, medium, large, flagship).
